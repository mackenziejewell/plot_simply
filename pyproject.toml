[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "plot_simply"
version = "0.1.0"
description = "Simple geographic plotting utilities"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name = "Your Name" }
]
dependencies = []